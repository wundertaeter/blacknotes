table:
  name: auth_user
  schema: public
array_relationships:
- name: notes
  using:
    foreign_key_constraint_on:
      column: user_id
      table:
        name: notes_note
        schema: public
- name: projects
  using:
    foreign_key_constraint_on:
      column: user_id
      table:
        name: notes_project
        schema: public
- name: spaces
  using:
    foreign_key_constraint_on:
      column: user_id
      table:
        name: notes_space
        schema: public
select_permissions:
- permission:
    allow_aggregations: true
    columns:
    - date_joined
    - email
    - first_name
    - id
    - is_active
    - is_staff
    - is_superuser
    - last_login
    - last_name
    - username
    filter: {}
  role: user
