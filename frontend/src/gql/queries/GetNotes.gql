query GetNotes($user_id: Int!, $project_id: uuid!) {
  notes: active_notes(where: {_and: {user_id: {_eq: $user_id}, project_id: {_eq: $project_id}, deleted: {_eq: false}}}) {
    id
    title
    content
    deadline
    done
  }
}