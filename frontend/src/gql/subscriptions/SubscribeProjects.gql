subscription SubscribeProjects($user_id: Int!) {
  projects: notes_project(where: {_and: {user_id: {_eq: $user_id}}, space_id: {_is_null: true}, default: {_eq: false}}) {
    id
    name
    icon
    default
  }
}
