subscription SubscribeLogbookProjects($user_id: Int!) {
  projects: notes_project(where: {_and: {user_id: {_eq: $user_id}, deleted: {_eq: false}, done: {_eq: true}}}) {
    title
    icon
    id
    done
    deleted
    updated_at
  }
}
