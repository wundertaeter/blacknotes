subscription SubscribeTrashNotes($user_id: Int!) {
  notes: notes_note(where: {_and: {user_id: {_eq: $user_id}, deleted: {_eq: true}}}) {
    id
    title
    content
    done
    deleted
    deadline
  }
}