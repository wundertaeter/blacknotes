mutation CreatePushSubscription($subscription: jsonb!, $user_id: uuid!) {
  insert_users_pushnotifications_one(object: {subscription: $subscription, user_id: $user_id}) {
    subscription
    user_id
  }
}